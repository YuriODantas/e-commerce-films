import styled from 'styled-components'

export const CartButtonRemoveContainer = styled.div`
  display: flex;
  align-items: center;
  justify-content: flex-end;

  > button {
    background-color: transparent;
    border: none;
    cursor: pointer;
  }
`
